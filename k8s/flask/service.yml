apiVersion: v1
kind: Service
metadata:
  name: flask
  namespace: flight-prediction
spec:
  selector:
    app: flask
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30501
  type: NodePort

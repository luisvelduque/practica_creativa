apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: flight-prediction
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30909
  type: NodePort
